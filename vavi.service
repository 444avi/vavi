[Unit]
Description=Vavi - Trump Truth Social market-awareness monitor
After=network-online.target
Wants=network-online.target
# Don't hammer on repeated crashes.
StartLimitIntervalSec=300
StartLimitBurst=5

[Service]
Type=simple
# Set for Amazon Linux (EC2). Adjust User/paths if you install elsewhere.
# Vavi is stdlib-only, so the system python3 is fine.
User=ec2-user
WorkingDirectory=/home/ec2-user/vavi
ExecStart=/usr/bin/python3 /home/ec2-user/vavi/vavi.py
Restart=always
RestartSec=30

[Install]
WantedBy=multi-user.target
